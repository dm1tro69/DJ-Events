import Layout from "../../components/Layout";


const EventPage = () => {

    return (
        <Layout title={'Add New Event'}>
            <h1>Event Page</h1>
        </Layout>
    );
};

export default EventPage;
